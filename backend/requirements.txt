# ---- LedgerRank backend dependencies (pinned for reproducibility) ----

# Web framework + ASGI server
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ORM + validation
SQLAlchemy==2.0.30
pydantic==2.7.4
pydantic-settings==2.3.4

# Testing
pytest==8.2.2
httpx==0.27.0            # async test client for FastAPI
